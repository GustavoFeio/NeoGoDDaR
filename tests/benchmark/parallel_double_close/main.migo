def main.main():
    let t0 = newchan main.main0.t0_chan0, 0;
    spawn main.worker1(t0);
    spawn main.worker2(t0);
def main.worker1$1(ch):
    send ch (main.go:9:6);
def main.worker1(ch):
    spawn main.worker1$1(ch);
    recv ch (main.go:11:2);
    close ch;

def main.worker2$1(ch):
    recv ch (main.go:17:3);
def main.worker2(ch):
    spawn main.worker2$1(ch);
    send ch (main.go:19:5);
    close ch;
