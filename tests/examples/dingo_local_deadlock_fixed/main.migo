def main.main():
    let t0 = newchan main.main0.t0_chan0, 0;
    let t1 = newchan main.main0.t1_chan0, 0;
    spawn main.Send(t0);
    spawn main.Recv(t0, t1);
    recv t1 (main.go:23:2);
def main.Send(ch):
    send ch (main.go:14:48);

def main.Recv(ch, done):
    recv ch (main.go:15:53) -> (main.go:15:50);
    send done (main.go:15:50);
